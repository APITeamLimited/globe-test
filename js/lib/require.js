"use strict";

function require(name) ***REMOVED***
	var mod = __internal__.modules[name];
	if (!mod) ***REMOVED***
		throw new Error("Unknown module: " + name);
	***REMOVED***
	return mod;
***REMOVED***
